<Window x:Class="IpisCentralDisplayController.views.SplPopUpStation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:IpisCentralDisplayController.views"
        xmlns:conv="clr-namespace:IpisCentralDisplayController.converters"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IpisCentralDisplayController.views"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800">


    <Window.Resources>
        <conv:RegionalLanguageToLabelConverter x:Key="RegionalLanguageToLabelConverter"/>
    </Window.Resources>
    <Window.DataContext>
        <views:StationViewModel/>
    </Window.DataContext>
    
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
               
            </Grid.ColumnDefinitions>

            <!-- Station List on the Left -->
            <GroupBox Header="Stations" Grid.Column="0" Margin="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Row="0">
                        <TextBlock Text="{Binding FieldText}" Width="80" VerticalAlignment="Center" Margin="2"/>
                        <TextBox Margin="2" Height="30" VerticalContentAlignment="Center" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
                    </DockPanel>
                    <ListView Grid.Row="1" Name="StationListView" ItemsSource="{Binding Stations}" SelectedItem="{Binding SelectedStation}" Margin="2" SelectionMode="Extended">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Code" DisplayMemberBinding="{Binding StationCode}"/>
                                <GridViewColumn Header="Name (English)" DisplayMemberBinding="{Binding StationNameEnglish}" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </GroupBox>

        </Grid>
        
        
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="2" Grid.Column="0" Grid.Row="1">
            <!--<Button Content="Add Station" Width="100" Height="30" Margin="2" Click="AddStationButton_Click"/>-->
            <!--<Button Content="Edit Station" Width="100" Height="30" Margin="2" Click="UpdateStationButton_Click"/>-->
            <!--<Button Content="Delete Selected" Width="100" Height="30" Margin="2" Click="DeleteStationButton_Click"/>-->
            <!--<Button Content="Delete All" Width="100" Height="30" Margin="2" Click="DeleteAllStationsButton_Click"/>-->
            <!--<Button Content="Import" Width="100" Height="30" Margin="2" Click="ImportButton_Click"/>
            <Button Content="Export" Width="100" Height="30" Margin="2" Click="ExportButton_Click"/>-->
            <Button Content="Use This" Width="100" Height="30" Margin="60,2,2,2" Click="UseThisButton_Click"/>
        </StackPanel>
        <TextBlock Grid.Row="2" x:Name="tb_status" Height="24" HorizontalAlignment="Center"/>
    </Grid>

</Window>
